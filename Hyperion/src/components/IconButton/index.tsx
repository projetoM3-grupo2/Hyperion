import { IconButton } from "@chakra-ui/react";

export const IconBtn = () => {
  return (
    <IconButton
      variant="outline"
      colorScheme="teal"
      aria-label="Call Sage"
      fontSize="20px"
      icon={undefined}
    />
  );
};
